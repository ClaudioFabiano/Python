# Your little brother has just learned multiplication tables in school.
# He needs a learned tables of 6 and 7.

I = int(input("Enter the number: "))  # Prompt the user to enter the number
print(f"\nMultiplication table of {I}:")
for i in range(1, 11):  # Iterate from 1 to 10 (inclusive)
    print(f"\t{i} x {I} =", i * I)  # Print the multiplication table
